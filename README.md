# 🚲 自行车越野 · 真实物理版

原生 JavaScript + Canvas 实现的物理越野自行车游戏，**零依赖、零构建**，打开网页即玩。

- 没有跳跃键：靠坡度冲起腾空，空中用 `←/→` 调整车身姿态落地
- 三质点刚体物理：油门翘头 / 刹车栽头 / 坡顶腾空 / 落地缓冲 / 陡坡打滑都是"算出来"的
- 12 个场景 / 12 条支线 × 6 关 = **72 关 + 最终任务**，6 种关卡变体，4 类机制性难度
- 比赛模式、排位赛（普通 / 高级）、无限模式（登顶后可自选场景）
- 金币、升级车间、车库、成就、打赏、移动端触摸操作、浏览器本地存档（可导出 / 导入）

## 🗺 内容与玩法

**场景（12）**：绿野、雪原、荒漠、月面、雨林、火山、冰川、红岩峡谷、沼泽、城市废墟、天空浮岛、极夜星空。
每个场景有各自的重力、抓地、天空/地表配色、装饰与环境特效（数据驱动，渲染层无 `theme === 数字` 硬编码）。

**关卡**：12 条支线 × 6 关 = 72 关，另加 1 关「最终任务」（超长赛道、多场景分段串联）。
支线内链式解锁、支线间可并行推进；难度（坡度 / 落差 / 颠簸 / 机制密度 / 三星时限）沿全局进度单调递增。
72 关全通后解锁「最终任务」，通关最终任务 → 收到比赛邀请 → 解锁排位赛。

**关卡变体（6）**：每条支线的第 3、5 关是特殊变体，其余为常规关。

| 变体 | 内容 |
|---|---|
| `normal` | 标准赛道 |
| `sprint` | 限时冲刺：门限极严、赛道无补给 |
| `gauntlet` | 障碍迷宫：障碍成簇密布，必须减速穿越 |
| `airtime` | 空翻挑战：需累计滞空 + 连招达标才算通关 |
| `fuelrun` | 燃料极限：赛道仅 1 个油罐，必须规划 |
| `downhill` | 极速下坡：危险段密集，须精准刹车 |

**机制性难度**：

- **障碍物**：高速撞上必摔车，可减速碾过或腾空飞越；不生成在出生点/终点缓冲区
- **危险段**：车身中点超速进入必摔车，有可提前预判的限速路标；低于阈值可安全通过
- **限时门**：每关 3~5 道，累计时限比三星放宽 20%；任一门超时本局判负，且不写入星级与解锁
- **摔车惩罚**：每次摔车扣 8% 燃料 + 2 秒计时惩罚（计入三星判定用时）

**玩法模式**：

- **闯关**：左 上角为支线任务面板，逐支线选关
- **比赛**：与 AI 对手在同一赛道竞速，先到终点者胜（胜 +300 金币）
- **排位赛**：普通 / 高级两档；胜负改变段位分（不低于 0），段位分 ≥ 1200 解锁高级赛，≥ 2400 视为登顶
- **无限模式**：未登顶为随机地形；登顶后可自选已通关场景（重力 / 抓地 / 地表 / 环境特效随之切换）

## 🎮 操作

| 按键 | 作用 |
|---|---|
| `→` / `D` | 加速（空中：顺时针转体 = 前空翻） |
| `←` / `A` | 刹车（空中：逆时针转体 = 后空翻） |
| `P` / `Esc` | 暂停 / 继续 |
| `R` | 重开当前局 |
| `U` | 升级车间 |
| `M` | 静音开关 |
| `+` / `-` | 缩放视野 |

手机/平板：右下角 `🎮` 可切换屏幕方向键，右上角 `⛶` 全屏。

## 🚀 运行

本项目使用 **原生 ES Module**，浏览器出于安全策略**不允许**用 `file://` 直接打开模块（会报 CORS 错误），
所以本地运行需要一个静态服务器（任选其一）：

```bash
# 方式 1：Python（推荐，随系统自带）
python -m http.server 8000
# 然后浏览器打开 http://127.0.0.1:8000/

# 方式 2：Node
npx serve .
```

## 🌐 部署到 GitHub Pages

推送到 GitHub 后，进入仓库 `Settings → Pages`，把 **Source** 设为 `Deploy from a branch`，
分支选 `main`、目录选 `/ (root)`，保存即可。无需任何构建步骤（没有打包、没有依赖）。

访问地址：`https://<你的用户名>.github.io/<仓库名>/`

## 📁 目录结构

```
.
├── index.html            # 只有 DOM 骨架（80 行，无内联 onclick / 无 <style>）
├── styles/main.css       # 全部样式
├── assets/qr.png         # 打赏二维码（可替换/删除）
├── src/
│   ├── main.js           # 入口：装配模块 + 启动固定步长主循环 + 自动存档
│   ├── config/           # 纯数据与地形数学（常量/主题/车辆/关卡）
│   │   ├── constants.js  #   ★ 标度唯一事实来源（px↔m、px/s↔km/h、物理与机制常量）
│   │   ├── themes.js     #   12 个场景：重力 / 抓地 / 天空 / 地表三色 / 纹理 / 装饰 / 背景图层
│   │   ├── vehicles.js   #   3 辆车：山地车 / 竞速车 / 越野车
│   │   └── levels.js     #   12 支线 × 6 关 + 最终任务 + 地形函数 + 真实坡度测量
│   ├── core/             # 运行时基础设施
│   │   ├── store.js      #   共享可变状态（唯一容器，避免循环依赖）
│   │   ├── loop.js       #   固定步长累加器（1/60 s）
│   │   ├── canvas.js     #   画布/视口/DPR
│   │   ├── input.js      #   键盘/触摸/全屏
│   │   ├── audio.js      #   WebAudio 合成音效
│   │   ├── storage.js    #   localStorage 存档 + 进度阶梯 + 迁移 + 导入/导出
│   │   ├── toast.js      #   屏幕提示
│   │   └── utils.js      #   clamp/lerp/伪随机等
│   ├── physics/          # 物理
│   │   ├── terrain.js    #   地形访问层
│   │   ├── bike.js       #   三质点刚体（约束/腾空/落地/摔车/障碍碰撞）
│   │   └── fuel.js       #   燃料
│   ├── game/             # 玩法
│   │   ├── game.js       #   状态机与结算（闯关/比赛/排位/无限）
│   │   ├── world.js      #   关卡构建（金币/油罐/加速带/障碍/危险段/门/跳台/装饰）
│   │   ├── race.js       #   AI 竞速（配速按三星节奏标定 + 追赶修正）
│   │   ├── stats.js      #   里程/滞空/特技结算
│   │   └── progress.js   #   金币与成就
│   ├── render/           # 渲染
│   │   ├── scene.js      #   一帧的总装
│   │   ├── background.js / terrain.js / bike.js / entities.js
│   │   ├── particles.js / camera.js / hud.js
│   └── ui/               # 界面
│       ├── menu.js / panels.js / shop.js / donate.js
├── tools/autotest.mjs    # 无头自动测试（Node 运行，不需要浏览器）
└── .trae/specs/          # 规范驱动开发的 spec / tasks / checklist 文档
```

依赖方向是单向的（无循环依赖）：

```
config/ + core/  →  physics/  →  game/  →  render/  →  ui/  →  main.js
```

`game/` 永不 `import` `ui/`：界面动作通过 `initGame(presenter)` 注入，因此模块图是严格无环的。

> 规范文档现行位置是 `.trae/specs/`（自 Trae 迁移而来）。`.specs/` 同样被识别，新写的文档优先放那里。

## 🏁 比赛模式的 AI 配速

AI 巡航速度按**本关三星要求均速 `den3`** 标定（而不是玩家极速 `MAXV`）——因为 `MAXV` 由车辆/升级决定，
不随关卡变难而提高；早期实现用 `MAXV×(1+42%×ramp)` 会让后期 AI 达到三星节奏的 2~3 倍，比赛几乎必输。

| 模式 | AI 配速（相对 `den3`） | 追赶修正 |
|---|---|---|
| 普通比赛 | `0.68×` | 有（`[0.80, 1.25]×`，AI 领先降速 / 落后提速） |
| 普通排位赛 | `0.70× → 0.90×`（随段位分） | 无（纯配速检验） |
| 高级排位赛 | `0.95× → 1.25×`（随段位分） | 无 |

普通比赛的配速上限为 `0.68 × 1.25 = 0.85 × den3 < 1`，因此**只要跑出三星水平就必定赢**；追赶修正保证对手
不会一路绝尘，也不会因一次小失误就被拉开。

## 🧪 自动测试

```bash
node tools/autotest.mjs              # 跑全部测试
node tools/autotest.mjs --modules    # 只检查 33 个模块能否加载
node tools/autotest.mjs --levels     # 只跑"72 关可通关"并打印逐关用时
node tools/autotest.mjs --diag=19    # 单关诊断（逐 5 秒打印位置/速度/摔车次数）
node tools/autotest.mjs --racediag   # 额外打印逐关"参考骑手 vs AI"比赛结果
```

它会用 DOM / Canvas / localStorage 打桩加载**真实游戏代码**（不是模拟），然后验证（共 374 项检查）：

- 33 个模块全部可加载（无循环依赖 / 初始化报错）
- 12 个场景逐帧渲染无异常；渲染层不存在 `theme === <数字>` 硬编码
- 恰好 12 支线 × 6 关 + 1 关最终任务；难度逐关单调递增；支线场景与关卡主题一致
- 6 种变体穿插排布正确、均出现；`airtime` 未达标不判通过；`fuelrun` 仅 1 罐且可通关
- 障碍物撞上必摔、腾空越过不摔；危险段超阈必摔、低于阈值安全；限时门超时判负且不写入星级/解锁
- 摔车两次后燃料 -16%、用时 +4s
- 72 关"刹车策略"参考骑手自动试跑**全部到达终点**；全油门骑手无法通过后 1/3 关卡（反向验收）
- **反向断言**：后 24 关中有 21 关全油门过不去；前 24 关保留教学余量
- 物理 / 油耗 / 计时与刷新率无关：`dt=1/60`、`1/120`、`1/144` 下位移差 `0.0000px`
- 落地冲击分级单调、≥0.9s 滞空可完成一圈空翻、倒立落地会摔车
- 比赛公平性：AI 基准配速慢于三星节奏、追赶有界；逐关实机跑参考骑手 70/72 关获胜
- 72 关三星时限物理可达且逐关收严（`den3` 单调不增）
- 进度阶梯：72 关全通才解锁最终任务；解锁阈值边界（1190/1210、2390/2410）
- 存档：全新存档仅支线 1 第 1 关解锁；老 20 关存档迁移到 72 关不回退且只执行一次；
  `bike_stars` 损坏时正常启动；`localStorage` 抛异常时游戏仍可运行
- 导入 / 导出：四字段齐全、`data` 覆盖全部 `bike_` 键、导出→重置→导入逐键一致、
  非法文件被拒且现有存档逐键不变
- 静态检查：无裸半标度换算（`*SUB` / `/SUB`）、import/export 完全对应、无死代码、
  `index.html` ≤200 行 / 无 `<style>` / 无内联 `onclick`

## 🎯 开发须知（标度纪律）

所有速度/距离换算集中在 `src/config/constants.js`：

| 量 | 约定 |
|---|---|
| 世界标度 | **100 px = 1 m** |
| 车速 | `px/s`，`toKmh()` 换算显示 |
| 物理步长 | 固定 `DT = 1/60 s`，每步 6 个子步；子步位移 × `SUBV` = 真实 px/s |
| 极速 | 平路 `MAXV`，下坡允许到 `1.35 × MAXV` |

> 历史教训：早期版本做过一次"速度标度重构"但只改了物理，渲染/AI/特技/粒子里残留了 6~7 处
> 旧常数（`*SUB`、`260*SUB`、`MAXV/SUB`…），造成"代码在、功能死"的假功能。
> 因此现在**任何阈值都必须写成真实 px/s**，禁止裸换算。

## 💾 存档

使用 `localStorage`（键名前缀 `bike_`，共 13 个键）：金币、每辆车独立的升级等级、解锁关卡、星级、
车辆、静音、无限模式最佳成绩、成就，以及进度阶梯（`bike_prog`）、段位分（`bike_rating`）、
累计统计（`bike_stat`：总局数 / 总里程 / 总时长 / 最后游玩时间）。

**自动保存时机**：通关结算、解锁关卡/场景、购车、升级、成就、段位变化立即写盘；骑行中每 30 秒兜底写盘；
切到后台（`visibilitychange`）时写盘。**清空浏览器数据会重置存档**。

**导入 / 导出**（菜单 → 💾 存档）：

- **导出**：把全部 `bike_` 键打包为
  `{ app: "dale-bike", format, savedAt, data }`，下载为 `dale-bike-save-<YYYYMMDD-HHmm>.json`
- **导入**：选择文件 → 完整校验（`app` / `format` / `data` 合法性）→ 展示"当前 vs 导入"对比
  （通关数 / 总星数 / 段位分 / 金币）→ 二次确认 → 整体覆盖后刷新界面。校验失败**不写入任何键**
- **重置**：二次确认后清空全部 `bike_` 键，回到"仅支线 1 第 1 关解锁"的初始状态

老存档（20 关结构）会在首次加载时自动迁移到 72 关结构（星级按全局索引映射、解锁不回退、星级数组补齐），
迁移由 `bike_v` 版本号守护，只执行一次。

## 📜 License

MIT
