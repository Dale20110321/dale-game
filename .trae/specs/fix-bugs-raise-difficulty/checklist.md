# Checklist

## 缺陷修复
- [x] 通关动画（800ms）期间按 `R` 重开，不会被推到下一关（自动断言通过：`通关后立即重开不会被推到下一关 → selLevel=0 state=play clearing=false`）
- [x] 无限模式燃料耗尽结算（1.6s）期间按 `R` 重开，不会被弹回主菜单（自动断言通过：`重开后上一局的延迟结算回调被作废 → called=0`）
- [x] 比赛模式对手先到终点（900ms）期间按 `R` 重开，不会串到下一关（自动断言通过：`三处延迟结算都经过单局世代守卫 → 匹配 3 处`）
- [x] `game.js` 中所有延迟结算回调都先校验单局世代号 `store.run.gen`
- [x] `crashMargin` 被倒立摔车判定读取，车架等级提升带来可观测的抗倒立差异（自动断言通过：`Lv0(crashMargin=4) 摔车=true(40步) / 满级(crashMargin=14) 摔车=false(901步)`）
- [x] `src/` 全目录不再出现 `bike.stunned` / `.stunned`（静态断言通过：`死状态 stunned 已清除 → 无`）
- [x] 骑行中打开升级车间不再推进 `store.time`，通关用时不受影响（自动断言通过：`车间打开时时钟与三星计时冻结 → time 596.083→596.083`）
- [x] "掉出地图"判定基准改为地形真实最低点，车在 `bottomY + 900` 之下时一个物理步内触发回退（自动断言通过：`rear.y=288.0 ground=300.0 spawnX=24.0 lastSafeX=24.0`）
- [x] 20 关三星曲线单调收严，且隐含要求均速落在 `[0.45, 0.75] × REF_SPEED`（自动断言通过：`den3 ∈ [234, 390]px/s`、`要求均速 374→260px/s（占基准极速 0.72→0.50，单调不增）`）

## 难度提升
- [x] 第 20 关实测最大坡度显著大于第 1 关，且全部关卡坡度 ≤ 70°（自动断言通过：`第1关 19.7° / 第20关 57.7°（比值 2.94×）`、`最大 57.7°`）
- [x] 20 关在自动骑手（全油门 + 空中修正）下全部到达终点，无 NaN，单关用时 ≤ 110s（自动断言通过：`通过 20/20`，最慢 59.9s）
- [x] 后期油罐数量收紧到 ≤ 3，每关至少 1 个油罐（自动断言通过：`最少 1 个`、`第20关 3 个`）
- [x] 比赛模式玩家/AI 用时比值落在 0.85~1.15（AI 不再是陪跑）（自动断言通过：`玩家 9.9s / AI 9.9s → 比值 1.003`）
- [x] 帧率无关性（1/60、1/120、1/144）仍然一致
- [x] 落地分级、空翻可达性、倒立摔车等既有物理断言仍全部通过

## 工程约束
- [x] `node tools/autotest.mjs` 全绿，断言总数 > 78（114 项，失败 0 项）
- [x] 静态检查仍全绿：无裸半标度换算、import/export 完全对应、无历史遗留死代码、index.html ≤ 200 行（77 行）
- [x] 无新增依赖、无构建步骤，仍可直接用静态服务器打开游玩
- [x] `README.md` 中与本次改动相关的说明已同步（三星口径 / 燃油余量 / AI 基准 / 断言数）